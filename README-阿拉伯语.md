!/wslpress-new-logo.svg

# WSLpress (WSLpress) قوة الردع · أجنحة خفيفة: إطار عمل مدونة حديث يعتمد على Next.js و Contentlayer و Tailwind، مُصمم خصيصًا لمستخدمي الصينية المبسطة، مع واجهة كاملة مكتوبة بالعربية وتوافق كامل مع النظام البيئي المحلي.

# WSLpress 🚀 ]()

قالب مدونة حديث خفيف الوزن وفعّال، مبني باستخدام Next.js 13 + Contentlayer + Tailwind CSS.

**إذا كنت بحاجة إلى إدراج الصور بسرعة، يمكنك استخدام الأداة التي نقدمها، فهي مفيدة جدًا!**
- مقال التعريف: /posts/wslpress-base64-image-tool
- استخدمها مباشرة من هنا: 🌐 

WSLpress (Wingsly Press) هو قالب مدونة مُصمم للمطورين ومنشئي المحتوى. يجمع بين أحدث تقنيات الواجهة الأمامية وتصميم مرئي أنيق، ويدعم الوضع الداكن، والتخطيط المتجاوب، والإدارة المرئية للمحتوى، مما يسمح لك بالتركيز على إنشاء المحتوى دون القلق بشأن التكوينات المعقدة.

تم التطوير بواسطة **@che-ui** | 🌐 

---

## ✨ الميزات الأساسية

- **تكنولوجيا حديثة**: يعتمد على Next.js 13 App Router، ويدعم ميزات حديثة مثل مكونات React Server ومجموعات المسارات (Route Groups)، وما إلى ذلك.
- **إدارة محتوى فعّالة**: تحليل تلقائي لملفات Markdown/MDX عبر Contentlayer، آمن للنوع ولا يتطلب أي تكوين يدوي.
- **واجهة مستخدم جميلة وسهلة الاستخدام**: تصميم متجاوب مدعوم بـ Tailwind CSS، مع قسم رئيسي (Hero) مصمم بعناية في الصفحة الرئيسية وبطاقات المقالات.
- **صديق لمحركات البحث (SEO)**: في أقل من 10 أيام من إطلاقه، احتل هذا الموقع المركز الأول في أوزان بحث Bing للإصدارات المحلية والدولية والخارجية.
- **الوضع الداكن**: يتبع تلقائيًا تفضيلات النظام، ويدعم أيضًا التبديل اليدوي، ويتكيف مع سيناريوهات القراءة المختلفة.
- **نظام إدارة محتوى مرئي (CMS)**: Netlify (Decap) CMS مدمج، يسمح للمستخدمين غير التقنيين بنشر/تحرير المقالات بسهولة (تحت الصيانة حاليًا بسبب الالتزامات الدراسية، وغير موصى بشدة بتشغيله).
- **تجربة تطوير مُحسنة**: تكوين ESLint، التحقق من أنواع TypeScript، فئات Tailwind المساعدة (Utility Classes) للتطوير السريع.
- **نشر بنقرة واحدة**: يتكامل بسلاسة مع منصات مثل Cloudflare Pages و Vercel و Netlify، وما إلى ذلك، مع عملية نشر بسيطة وفعالة.
- **RSS**: يدعم الاشتراك في RSS (/rss.xml).
- **بحث عالمي**: يدعم البحث السريع في النص الكامل.

---

## 🚀 البدء السريع

### المتطلبات الأساسية

- Node.js 18.x أو أعلى
- pnpm 8.x أو أعلى (موصى به، يمكن أيضًا استخدام npm/yarn)

### الخطوة 1: استنساخ المستودع (Repository)

```Bash
git clone
cd wslpress
```

### الخطوة 2: تثبيت التبعيات (Dependencies)

```Bash
pnpm install
# أو npm install / yarn install
```

### الخطوة 3: تشغيل خادم التطوير

```Bash
pnpm dev
```

### الخطوة 4: الوصول إلى الموقع المحلي

افتح متصفحك وقم بزيارة `` لرؤية معاينة المدونة.

### الخطوة 5: بناء نسخة الإنتاج (Production Build)

```Bash
pnpm build
# بعد البناء، يمكنك بدء خادم الإنتاج باستخدام pnpm start
```

---

## 📝 دليل الاستخدام

### 1. إنشاء أول مقالة لك

1.  أنشئ ملف Markdown/MDX في دليل `content/` (على سبيل المثال، `hello-world.md`).
2.  أضف بيانات وصفية (Metadata) باتباع تنسيق *frontmatter* التالي:

```Markdown
---
title: "مدونتي الأولى"
date: "2026-01-21"
description: "هذه هي مقالتي الأولى المنشورة باستخدام WSLpress"
tags: ["Next.js", "التدوين"]
---

هنا محتوى المقالة الرئيسي. يمكنك استخدام صيغة Markdown أو MDX.

## عنوان فرعي
- عنصر قائمة 1
- عنصر قائمة 2

import SomeComponent from "@/components/SomeComponent";
<SomeComponent />
```

1.  بعد الحفظ، سيقوم خادم التطوير بإعادة التحميل الساخن (Hot Reload)، وستظهر المقالة على الفور في الصفحة الرئيسية وفي قائمة المقالات.

### 2. تخصيص إعدادات المدونة

-   **تعديل معلومات الموقع**: حرر `siteMetadata` في ملف `contentlayer.config.js` (يجب تحديث تكوين CMS وفقًا لذلك).
-   **ضبط الأنماط**: عدّل ملف `tailwind.config.js` لتخصيص الألوان والخطوط والمسافات، وما إلى ذلك.
-   **تغيير هيكل الصفحات**: حرر مكونات الصفحات في دليل `app/` (على سبيل المثال، `app/page.tsx` يتوافق مع الصفحة الرئيسية).
-   **إضافة مكونات**: أنشئ مكونات مخصصة في دليل `components/`، والتي يمكن استيرادها مباشرة في ملفات MDX.

### 3. إدارة المحتوى باستخدام Netlify CMS

1.  بعد تشغيل خادم التطوير، قم بزيارة `/admin`.
2.  سجّل الدخول باستخدام حساب Netlify الخاص بك (يتطلب ربطًا بمستودع في أول استخدام).
3.  في الواجهة الخلفية (Backend) لـ CMS، يمكنك إنشاء وتحرير وحذف المقالات مباشرة دون الحاجة إلى تعديل ملفات Markdown يدويًا.

---

## 📂 هيكل الدليل

```Plain Text
wslpress/
├── app/                  # دليل Next.js 13 App Router
│   ├── page.tsx         # الصفحة الرئيسية
│   ├── posts/           # صفحات قائمة/تفاصيل المقالات
│   └── layout.tsx       # التخطيط العام (يتضمن التنقل، التذييل)
├── components/          # مكونات قابلة لإعادة الاستخدام (بطاقات المقالات، شريط التنقل، إلخ)
├── content/             # دليل محتوى المقالات (ملفات Markdown/MDX)
├── public/              # موارد ثابتة (صور، أيقونات، تكوين CMS)
│   ├── admin/           # تكوين واجهة إدارة Netlify CMS
│   └── WSLpress.svg     # شعار المشروع
├── contentlayer.config.js # تكوين Contentlayer (أنواع المحتوى، بيانات وصفية)
├── tailwind.config.js   # تكوين Tailwind CSS
├── next.config.js       # تكوين Next.js
├── tsconfig.json        # تكوين TypeScript
└── package.json         # إدارة التبعيات
```

---

## 🌐 دليل النشر السريع (بعد عمل Fork لهذا المستودع)

### الخيار 1: النشر على Vercel (موصى به)

مثال: /

1.  قم بزيارة  وقم بتسجيل الدخول.
2.  انقر على "New Project" وقم باستيراد مستودع `wslpress`.
3.  لا حاجة لإعدادات إضافية؛ سيتعرف Vercel عليه تلقائيًا كمشروع Next.js.
4.  انقر على "Deploy" وانتظر اكتمال النشر. ستحصل على عنوان على الإنترنت.
5.  يمكنك ربط نطاق مخصص (اختياري). ملاحظة: لا يمكن للمستخدمين في الصين الرئيسية الوصول إلى نطاقات `.vercel.app`.

### الخيار 2: النشر على Netlify

مثال:

1.  قم بزيارة  وقم بتسجيل الدخول.
2.  انقر على "Add new site" → "Import an existing project".
3.  قم باستيراد مستودع `wslpress`.
4.  إعدادات البناء (يتم ملؤها تلقائيًا، عادة لا تحتاج إلى تغيير):
    - Build command: `pnpm build`
    - Publish directory: `.next`
5.  انقر على "Deploy site" لإكمال النشر.
6.  ستحصل على نطاق فرعي `.netlify.app`. اعتبارًا من 3 فبراير 2026، يمكن الوصول إليه في الصين، ولكن الوصول عبر شبكات China Mobile الخلوية قد يكون غير مستقر.

### الخيار 3: الطريقة القياسية للنشر على Cloudflare Pages (ليس Worker!)

مثال:

1.  قم بزيارة  وقم بتسجيل الدخول.
2.  انقر على "Compute and AI" → "Workers & Pages"، ثم انقر في أعلى اليمين على "Create an application". في الصفحة التالية، انقر على "Get started" تحت "Ready to deploy Pages?".
3.  قم باستيراد مستودع `wslpress`.
4.  تكوين البناء (اختر Next.js):
5.  انقر على "Deploy site" لإكمال النشر.
6.  ستحصل على نطاق فرعي `.pages.dev`، وهو محظور. ومع ذلك، يمكنك ربط نطاق مخصص.
7.  ثم اتبع هذا البرنامج التعليمي: /posts/deploying-wsl-on-cfpages

### الخيار 4: النشر على Cloudflare Pages عبر التحميل المباشر للأصول (موصى به للمبتدئين بدون وصول إلى الإنترنت العلمي)

راجع البرنامج التعليمي: /posts/deploy-wsl-on-cf-easily

---

## 🛠️ تفاصيل التكوين

### تكوين Contentlayer

يحدد `contentlayer.config.js` هيكل البيانات الوصفية للمقالات. يمكنك توسيع الحقول حسب الحاجة (مثل `author`، `coverImage`):

```JavaScript
export const Post = defineDocumentType(() => ({
  name: "Post",
  filePathPattern: `**/*.md`,
  fields: {
    title: { type: "string", required: true },
    date: { type: "date", required: true },
    description: { type: "string", required: true },
    tags: { type: "list", of: { type: "string" } },
    // يمكنك إضافة حقول مخصصة
    // author: { type: "string", required: false },
  },
  computedFields: {
    slug: {
      type: "string",
      resolve: (doc) => doc._raw.sourceFileName.replace(/\.md$/, "")
    },
  },
}));
```

### سمة Tailwind المخصصة

عدّل حقل `theme` في `tailwind.config.js` لتخصيص ألوان العلامة التجارية والخطوط وما إلى ذلك:

```JavaScript
/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class", // يدعم تبديل الوضع الداكن بناءً على الفئات
  theme: {
    extend: {
      colors: {
        primary: "#6366f1", // اللون الأساسي المخصص
      },
      fontFamily: {
        sans: ["Inter", "system-ui", "sans-serif"],
      },
    },
  },
  plugins: [],
};
```

---

## 🤝 إرشادات المساهمة

نرحب بالمساهمات من خلال الطرق التالية:

1.  قم بعمل Fork للمستودع وأنشئ فرعًا (`git checkout -b feature/xxx`).
2.  قم بتسجيل تغييراتك (`git commit -m "feat: add xxx feature"`).
3.  ادفع الفرع (`git push origin feature/xxx`).
4.  قدّم طلب سحب (Pull Request).

### معايير المساهمة

-   يتبع نمط الكود تكوين ESLint.
-   يجب أن تكون الميزات الجديدة مصحوبة بتحديثات لملف README.
-   يجب أن تستخدم رسائل الالتزام (Commit) التنسيق التقليدي (feat/fix/docs/style/refactor/test/chore).

---

## 📄 الترخيص

هذا المشروع مرخص بموجب رخصة GNU General Public License v3.0 - راجع ملف LICENSE للحصول على التفاصيل.

---

## 📞 اتصل بنا والإعلان الرسمي

إذا كان لديك أي أسئلة أو اقتراحات، يرجى الاتصال بنا عبر:

-   مشكلات GitHub (GitHub Issues): https:///che-ui/wslpress/issues
-   منصة الملاحظات الرسمية (موصى به): 🌐 /feedback.html
-   المطور: che-ui (طالب إعدادي في الصين، حافظ مستودع WSLpress على GitHub)

---

🌟 إذا أعجبك هذا القالب، يرجى منح المستودع نجمة (Star) لدعمنا!

---

**إعلان رسمي بشأن اسم المشروع "WSLPress"**

يعلن مشروع **WSLPress** هذا (إطار عمل لتوليد مواقع/مدونات ثابتة) رسميًا ما يلي:

**ليس له أي تبعية أو ارتباط أو صلة بتقنية ومنتج Windows Subsystem for Linux (WSL) من Microsoft.**

للتجنب أي لبس، تم توضيح النقاط التالية:

1.  **توضيح الاسم**: إن "WSL" في اسم هذا المشروع هو مجرد معرف ويمثل "威慑力" (قوة الردع). لا يعني ذلك أن المشروع يعتمد على تقنية Windows WSL ليعمل. إنه ببساطة إطار عمل لمدونة ثابتة.
2.  **بيان عدم الانتماء**: لم يتم تطوير هذا المشروع أو رعايته أو الموافقة عليه من قبل Microsoft. إن "WSL" الخاص بـ Microsoft هو علامته التجارية المستقلة لتقنية طبقة توافق نظام التشغيل.
3.  **استقلالية المشروع**: WSLPress هو أداة مفتوحة المصدر مستقلة لتوليد المواقع الثابتة. تصميمه وتطويره وصيانته لا علاقة لهما تمامًا بـ Microsoft.
4.  **أصل الاسم**: جاء الإلهام الأولي للاسم من "Wingsly"، مما يعني الخفة كالأجنحة، وليس له أي علاقة بـ Microsoft.
5.  **نطاق الاستخدام**: يمكن للمستخدمين استخدام هذا الإطار على أي منصة تدعم بيئة التنفيذ الخاصة به، دون قيود على Windows أو WSL.

نحن نحترم العلامات التجارية وحقوق الملكية الفكرية لـ Microsoft فيما يتعلق بتقنية WSL الخاصة به. أي سوء فهم محتمل ناتج عن تشابه الأسماء غير مقصود.

لأي استفسارات، يرجى الاتصال بنا عبر مستودع GitHub الخاص بالمشروع.

مع خالص التقدير،

**حافظ مشروع WSLPress: @che-ui**
