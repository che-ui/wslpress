!/wslpress-new-logo.svg

# WSLpress (WSLpress) Сдерживающая сила·Лёгкие крылья: Современный фреймворк для блога на основе Next.js, Contentlayer и Tailwind, созданный для пользователей упрощённого китайского языка, с полностью локализованным интерфейсом и полной совместимостью с локальной экосистемой.

# WSLpress 🚀 ]()

Лёгкий и эффективный современный шаблон для блога, созданный с использованием Next.js 13 + Contentlayer + Tailwind CSS.

**Если вам нужно быстро вставить изображения, вы можете использовать предоставленный нами инструмент, он очень полезен!**
- Вводная статья: /posts/wslpress-base64-image-tool
- Использовать напрямую: 🌐 

WSLpress (Wingsly Press) — это шаблон для блога, разработанный для разработчиков и создателей контента. Он сочетает в себе передовые фронтенд-технологии с элегантным визуальным дизайном, поддерживает тёмный режим, адаптивный макет и визуальное управление контентом, позволяя вам сосредоточиться на создании контента, не беспокоясь о сложных настройках.

Разработано **@che-ui** | 🌐 

---

## ✨ Ключевые особенности

- **Современный технологический стек**: Основан на Next.js 13 App Router, поддерживает современные функции, такие как React Server Components, группы маршрутов и т.д.
- **Эффективное управление контентом**: Автоматический разбор Markdown/MDX через Contentlayer, типобезопасность и не требует ручной настройки.
- **Красивый и удобный интерфейс**: Адаптивный дизайн на базе Tailwind CSS, с хорошо продуманной главной страницей (Hero-секция) и карточками статей.
- **Дружелюбность к SEO**: В течение менее 10 дней с момента запуска этот сайт занял 1-е место по весу в поиске Bing для национальной, международной и зарубежной версий.
- **Тёмный режим**: Автоматически следует системным настройкам, также поддерживает ручное переключение, адаптируется к различным сценариям чтения.
- **Визуальная CMS**: Интегрированная Netlify (Decap) CMS, позволяет нетехническим пользователям легко публиковать/редактировать статьи (В настоящее время находится на обслуживании из-за учебных обязательств, крайне не рекомендуется для включения).
- **Оптимизированный опыт разработки**: Конфигурация ESLint, проверка типов TypeScript, утилитарные классы Tailwind для быстрой разработки.
- **Развёртывание в один клик**: Легко адаптируется к платформам, таким как Cloudflare Pages, Vercel, Netlify и др., с простым и эффективным процессом развёртывания.
- **RSS**: Поддерживает RSS-подписки (/rss.xml).
- **Глобальный поиск**: Поддерживает быстрый полнотекстовый поиск.

---

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18.x или выше
- pnpm 8.x или выше (рекомендуется, также можно использовать npm/yarn)

### Шаг 1: Клонирование репозитория

```Bash
git clone
cd wslpress
```

### Шаг 2: Установка зависимостей

```Bash
pnpm install
# или npm install / yarn install
```

### Шаг 3: Запуск сервера разработки

```Bash
pnpm dev
```

### Шаг 4: Доступ к локальному сайту

Откройте браузер и перейдите по адресу ``, чтобы увидеть предварительный просмотр блога.

### Шаг 5: Сборка для продакшена

```Bash
pnpm build
# После сборки вы можете запустить продакшен-сервер с помощью pnpm start
```

---

## 📝 Руководство по использованию

### 1. Создание вашей первой статьи

1. Создайте файл Markdown/MDX в каталоге `content/` (например, `hello-world.md`).
2. Добавьте метаданные, следуя формату *frontmatter* ниже:

```Markdown
---
title: "Мой первый пост в блоге"
date: "2026-01-21"
description: "Это моя первая статья, опубликованная с использованием WSLpress"
tags: ["Next.js", "Блоггинг"]
---

Здесь находится основной текст статьи. Вы можете использовать синтаксис Markdown или MDX.

## Подзаголовок
- Элемент списка 1
- Элемент списка 2

import SomeComponent from "@/components/SomeComponent";
<SomeComponent />
```

1. После сохранения сервер разработки выполнит горячую перезагрузку, и статья появится в реальном времени на главной странице и в списке статей.

### 2. Настройка конфигурации блога

- **Изменение информации о сайте**: Отредактируйте `siteMetadata` в `contentlayer.config.js` (конфигурацию CMS необходимо обновить соответствующим образом).
- **Настройка стилей**: Измените `tailwind.config.js`, чтобы настроить цвета, шрифты, отступы и т.д.
- **Изменение структуры страниц**: Редактируйте компоненты страниц в каталоге `app/` (например, `app/page.tsx` соответствует главной странице).
- **Добавление компонентов**: Создавайте пользовательские компоненты в каталоге `components/`, которые можно напрямую импортировать в MDX-файлы.

### 3. Управление контентом с помощью Netlify CMS

1. После запуска сервера разработки посетите `/admin`.
2. Войдите в систему с помощью вашей учётной записи Netlify (при первом использовании требуется привязка к репозиторию).
3. В бэкенде CMS вы можете напрямую создавать, редактировать и удалять статьи без ручного редактирования файлов Markdown.

---

## 📂 Структура каталогов

```Plain Text
wslpress/
├── app/                  # Каталог Next.js 13 App Router
│   ├── page.tsx         # Главная страница
│   ├── posts/           # Страницы списка/деталей статей
│   └── layout.tsx       # Глобальный макет (включает навигацию, подвал)
├── components/          # Повторно используемые компоненты (карточки статей, панель навигации и т.д.)
├── content/             # Каталог контента статей (файлы Markdown/MDX)
├── public/              # Статические ресурсы (изображения, иконки, конфигурация CMS)
│   ├── admin/           # Конфигурация интерфейса администратора Netlify CMS
│   └── WSLpress.svg     # Логотип проекта
├── contentlayer.config.js # Конфигурация Contentlayer (типы контента, метаданные)
├── tailwind.config.js   # Конфигурация Tailwind CSS
├── next.config.js       # Конфигурация Next.js
├── tsconfig.json        # Конфигурация TypeScript
└── package.json         # Управление зависимостями
```

---

## 🌐 Руководство по быстрому развёртыванию (после форка этого репозитория)

### Вариант 1: Развёртывание на Vercel (рекомендуется)

Пример: /

1. Посетите  и войдите в систему.
2. Нажмите "New Project" и импортируйте репозиторий `wslpress`.
3. Дополнительная конфигурация не требуется; Vercel автоматически распознает его как проект Next.js.
4. Нажмите "Deploy" и дождитесь завершения развёртывания. Вы получите онлайн-адрес.
5. При желании можно привязать собственный домен. Примечание: пользователи в материковом Китае не могут получить доступ к доменам `.vercel.app`.

### Вариант 2: Развёртывание на Netlify

Пример:

1. Посетите  и войдите в систему.
2. Нажмите "Add new site" → "Import an existing project".
3. Импортируйте репозиторий `wslpress`.
4. Настройки сборки (заполняются автоматически, обычно изменения не требуются):
   - Build command: `pnpm build`
   - Publish directory: `.next`
5. Нажмите "Deploy site", чтобы завершить развёртывание.
6. Вы получите субдомен `.netlify.app`. По состоянию на 3 февраля 2026 года он доступен в Китае, но доступ через сети сотовой связи China Mobile может быть нестабильным.

### Вариант 3: Стандартный метод развёртывания на Cloudflare Pages (Не Worker!)

Пример:

1. Посетите  и войдите в систему.
2. Нажмите "Compute and AI" → "Workers & Pages", затем в правом верхнем углу нажмите "Create an application". На следующей странице нажмите "Get started" под "Ready to deploy Pages?".
3. Импортируйте репозиторий `wslpress`.
4. Конфигурация сборки (выберите Next.js):
5. Нажмите "Deploy site", чтобы завершить развёртывание.
6. Вы получите субдомен `.pages.dev`, который заблокирован. Однако вы можете привязать собственный домен.
7. Затем следуйте этому руководству: /posts/deploying-wsl-on-cfpages

### Вариант 4: Развёртывание на Cloudflare Pages путём прямой загрузки ресурсов (рекомендуется для новичков без доступа к "научному интернету")

См. руководство: /posts/deploy-wsl-on-cf-easily

---

## 🛠️ Детали конфигурации

### Конфигурация Contentlayer

`contentlayer.config.js` определяет структуру метаданных для статей. Вы можете расширять поля по мере необходимости (например, `author`, `coverImage`):

```JavaScript
export const Post = defineDocumentType(() => ({
  name: "Post",
  filePathPattern: `**/*.md`,
  fields: {
    title: { type: "string", required: true },
    date: { type: "date", required: true },
    description: { type: "string", required: true },
    tags: { type: "list", of: { type: "string" } },
    // Вы можете добавлять пользовательские поля
    // author: { type: "string", required: false },
  },
  computedFields: {
    slug: {
      type: "string",
      resolve: (doc) => doc._raw.sourceFileName.replace(/\.md$/, "")
    },
  },
}));
```

### Пользовательская тема Tailwind

Измените поле `theme` в `tailwind.config.js`, чтобы настроить цвета бренда, шрифты и т.д.:

```JavaScript
/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class", // Поддерживает переключение тёмного режима на основе классов
  theme: {
    extend: {
      colors: {
        primary: "#6366f1", // Пользовательский основной цвет
      },
      fontFamily: {
        sans: ["Inter", "system-ui", "sans-serif"],
      },
    },
  },
  plugins: [],
};
```

---

## 🤝 Рекомендации по внесению вклада

Вклад приветствуется следующими способами:

1. Сделайте форк репозитория и создайте ветку (`git checkout -b feature/xxx`).
2. Зафиксируйте свои изменения (`git commit -m "feat: add xxx feature"`).
3. Отправьте ветку (`git push origin feature/xxx`).
4. Отправьте Pull Request.

### Стандарты вклада

- Стиль кода соответствует конфигурации ESLint.
- Новые функции должны сопровождаться обновлениями README.
- Сообщения о фиксации должны использовать обычный формат коммитов (feat/fix/docs/style/refactor/test/chore).

---

## 📄 Лицензия

Этот проект лицензирован под GNU General Public License v3.0 - подробности см. в файле LICENSE.

---

## 📞 Свяжитесь с нами & Официальное заявление

Если у вас есть вопросы или предложения, пожалуйста, свяжитесь с нами через:

- GitHub Issues: https:///che-ui/wslpress/issues
- Официальная платформа обратной связи (Рекомендуется): 🌐 /feedback.html
- Разработчик: che-ui (ученик средней школы в Китае, сопровождающий репозитория WSLpress GitHub)

---

🌟 Если вам нравится этот шаблон, пожалуйста, поставьте звёздочку репозиторию, чтобы поддержать нас!

---

**Официальное заявление относительно названия проекта "WSLPress"**

Настоящим проект **WSLPress** (фреймворк для генерации статических сайтов/блогов) заявляет:

**Он не имеет никакой принадлежности, зависимости или связи с технологией и продуктом Windows Subsystem for Linux (WSL) от Microsoft.**

Во избежание путаницы разъясняются следующие моменты:

1.  **Пояснение названия**: "WSL" в названии этого проекта является лишь идентификатором и означает "威慑力" (Сдерживающая сила). Это не означает, что проект зависит от технологии Windows WSL для функционирования. Это просто фреймворк для статического блога.
2.  **Заявление об отсутствии связи**: Этот проект не разработан, не спонсируется и не одобрен Microsoft. "WSL" от Microsoft является его независимым товарным знаком для технологии уровня совместимости операционной системы.
3.  **Независимость проекта**: WSLPress — это независимый инструмент с открытым исходным кодом для генерации статических сайтов. Его дизайн, разработка и обслуживание полностью не связаны с Microsoft.
4.  **Происхождение названия**: Первоначальное вдохновение для названия пришло от "Wingsly", что подразумевает лёгкость, как крылья, и не связано с Microsoft.
5.  **Область применения**: Пользователи могут использовать этот фреймворк на любой платформе, поддерживающей его среду выполнения, без ограничений Windows или WSL.

Мы уважаем товарные знаки и права на интеллектуальную собственность Microsoft на его технологию WSL. Любое возможное недопонимание, возникшее из-за сходства названий, является непреднамеренным.

По любым вопросам обращайтесь к нам через репозиторий GitHub проекта.

С уважением,

**Сопровождающий проекта WSLPress: @che-ui**
